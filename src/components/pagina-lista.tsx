import { Cabecalho } from "./cabecalho"; import { Cartao } from "./cartao"; import { Plus, Search } from "lucide-react";
export function PaginaLista({ titulo, subtitulo, acao, vazio, children }: { titulo: string; subtitulo: string; acao: string; vazio: string; children?: React.ReactNode }) { return <><Cabecalho titulo={titulo} subtitulo={subtitulo}/><div className="barra-busca"><Search size={18}/><input placeholder={`Buscar ${titulo.toLowerCase()}...`} aria-label="Buscar"/></div><button className="botao primario"><Plus size={18}/>{acao}</button><div className="grade"><Cartao><div className="vazio"><span>✦</span><h2>Nada por aqui ainda</h2><p>{vazio}</p></div>{children}</Cartao></div></>; }
