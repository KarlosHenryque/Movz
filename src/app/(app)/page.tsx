import Link from"next/link";import{ArrowUpRight,Dumbbell,WalletCards}from"lucide-react";import{Cabecalho}from"@/components/cabecalho";import{Cartao}from"@/components/cartao";import{exigirUsuario}from"@/lib/autenticacao/usuario";
const dinheiro=(v:number)=>v.toLocaleString("pt-BR",{style:"currency",currency:"BRL"});
export default async function Page(){const{supabase,user}=await exigirUsuario();const inicio=new Date();inicio.setDate(1);const[{data:treino},{data:despesas},{count:execucoes},{data:series}]=await Promise.all([supabase.from("treinos").select("id,nome,descricao").eq("usuario_id",user.id).eq("ativo",true).limit(1).maybeSingle(),supabase.from("despesas").select("valor").eq("usuario_id",user.id).gte("data",inicio.toISOString().slice(0,10)),supabase.from("execucoes_treino").select("id",{count:"exact",head:true}).eq("usuario_id",user.id).eq("status","CONCLUIDA").gte("iniciada_em",inicio.toISOString()),supabase.from("series_executadas").select("volume").eq("usuario_id",user.id).gte("concluida_em",inicio.toISOString())]);const total=despesas?.reduce((s,d)=>s+Number(d.valor),0)??0;const volume=series?.reduce((s,d)=>s+Number(d.volume),0)??0;return <><Cabecalho titulo="Olá! 👋" subtitulo="Vamos cuidar do que importa hoje?"/><div className="grade"><Cartao destaque><div className="linha"><span className="selo">PRÓXIMO TREINO</span><Dumbbell/></div><h2>{treino?.nome??"Nenhum treino ativo"}</h2><p>{treino?.descricao||"Crie uma ficha para começar."}</p><Link className="botao claro" href="/treinos">{treino?"Abrir treinos":"Criar treino"} <ArrowUpRight size={18}/></Link></Cartao><Cartao titulo="Financeiro"><div className="linha"><div><p className="muted">Gastos no mês</p><strong className="valor">{dinheiro(total)}</strong></div><WalletCards/></div><Link href="/financeiro">Ver detalhes →</Link></Cartao><Cartao titulo="Atividade"><div className="metricas"><div><strong>{execucoes??0}</strong><span>treinos no mês</span></div><div><strong>{volume.toLocaleString("pt-BR")} kg</strong><span>volume total</span></div></div></Cartao></div></>}
